import { DropDownContainerStyle } from "./drop.style";

const DropDownContainer = ({ children }) => {
  return <DropDownContainerStyle>{children}</DropDownContainerStyle>;
};

export default DropDownContainer;
